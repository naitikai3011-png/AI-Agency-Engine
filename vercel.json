{
  "buildCommand": "pnpm install && pnpm --filter @workspace/agency-engine run build && cp -r artifacts/agency-engine/dist ./dist",
  "outputDirectory": "dist",
  "framework": null,
  "env": {
    "BASE_PATH": "/"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
